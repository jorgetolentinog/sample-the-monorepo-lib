import React from "react";
import ReactDOM from "react-dom/client";
import { TheMonorepoDemo } from ".";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TheMonorepoDemo />
  </React.StrictMode>
);
